# Privacy boundary

The React Native Web app has no login and no server-side worksheet-answer storage.

Future worksheet saving should remain browser-local unless the project deliberately changes that privacy model.
