# React Native Web migration

The app now runs as a Vite-powered React Native Web static app.

Current app code lives under `src/`.

The exercise catalog is in `src/exercises/catalog.js`.

Static browser assets are in `public/` and are copied into the Vite `dist` output.
