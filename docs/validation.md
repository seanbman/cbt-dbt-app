# Validation

Run these checks for the React Native Web app:

```bash
npm install
npm test
npm run build
```

CI performs the same checks and then serves the built `dist` directory to confirm static routes return the app shell.
