# Exercise catalog

This directory contains the static exercise library used by the React Native Web app.

The catalog is validated by Node tests. User worksheet answers are not stored here and are not sent to a server.
