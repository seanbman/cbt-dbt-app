# App source

React Native Web source code for Steady Steps lives here.

`App.jsx` owns the screen rendering and client navigation.

`routing.js` keeps route parsing and query-string handling separate from rendering.
